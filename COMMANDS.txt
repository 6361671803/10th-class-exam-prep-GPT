# Initialize local repo, commit, and push to GitHub
git init
git add .
git commit -m "Initial commit: exam-prep-gpt behavior export"
# create repo on GitHub (via web UI) and copy the remote URL, then:
git remote add origin https://github.com/<USERNAME>/<REPO>.git
git branch -M main
git push -u origin main

# If using SSH:
# git remote add origin git@github.com:<USERNAME>/<REPO>.git
# git push -u origin main
